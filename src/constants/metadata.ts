export enum MetadataKey {
  USER_ID_PATH = 'starsuper.userIdPath',
  PERMISSIONS = 'starsuper.permissions',
  CUSTOM_CACHE_TTL = 'startsuper.customCacheTtl',
}

// Indicates that URL will be used for cache key
export const CACHE_KEY_BY_URL = 'starsuper.cacheKeyByUrl';
